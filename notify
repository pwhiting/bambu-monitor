# Just send notification (no output)
notify "PrintComplete"

# Send notification and pass through a value
notify "PrintComplete" "/path/to/video.mp4"

# Use in a loop
loop -e mp4 -d ~/videos | notify "NewVideo"